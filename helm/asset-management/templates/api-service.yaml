apiVersion: v1
kind: Service
metadata:
  name: asset-api
  labels:
    {{- include "asset-management.labels" . | nindent 4 }}
spec:
  selector:
    app: asset-api
  ports:
    - port: {{ .Values.backend.service.port }}
      targetPort: 3000