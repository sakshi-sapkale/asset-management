apiVersion: v1
kind: Service
metadata:
  name: asset-frontend
  labels:
    {{- include "asset-management.labels" . | nindent 4 }}
spec:
  selector:
    app: asset-frontend
  ports:
    - name: http
      port: {{ .Values.frontend.service.port }}
      targetPort: 80